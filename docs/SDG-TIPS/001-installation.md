# Installation

SDG-TIPS is installed automatically as part of SDG-TERM. No separate install step is needed.

Once installed, `tipme` and `sdgtip` commands are available, and tips appear on terminal startup.
