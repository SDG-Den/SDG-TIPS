# Installation

```bash
sdgpkg install sdg-tips
```

This installs the `sdgtip`, `tipme`, and `alltips` commands and copies tip files to `~/.local/tips/`.

## Part of SDG-OS-META

If you installed the `sdg-os-meta` metapackage, SDG-TIPS is included automatically and no separate install step is needed.
